<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= @campsite.name %> | OnsenCamp</title>

    <!-- OGP -->
    <meta property="og:title" content="<%= @campsite.name %>" />
    <meta property="og:image" content="<%= @campsite.images.attached? ? url_for(@campsite.images.first) : image_url('default_ogp.png') %>" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:type" content="article" />

    <!-- Twitterカード -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= @campsite.name %>">
    <meta name="twitter:image" content="<%= @campsite.images.attached? ? url_for(@campsite.images.first) : image_url('default_ogp.png') %>">
  </head>
  <body>
    <p>このページはSNSシェア用です</p>
  </body>
</html>
