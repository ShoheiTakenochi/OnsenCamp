# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
one:
  email: one@example.com
  # password_digest: <%= BCrypt::Password.create('secret') %> # この行を削除
  password: 'secret' # テストで使用するためのプレーンなパスワード (devise が処理します)

two:
  email: two@example.com
  # password_digest: <%= BCrypt::Password.create('password') %> # この行を削除
  password: 'password' # テストで使用するためのプレーンなパスワード (devise が処理します)